@using FinanceHelper.Web.Models.Finance
@model FinanceModel

@{
    ViewData["Title"] = "Finance Dashboard";
}

<div class="py-3">

    @if (Model?.Salary == null)
    {
        @await Html.PartialAsync("_salaryInput", new SalaryInputModel())
    }
    else
    {
        @* ===============================
           HEADER + EDIT BUTTON
           =============================== *@
        <div class="d-flex justify-content-between align-items-center mb-3">
            <div>
                <h3 class="fw-bold mb-0">Finance Dashboard</h3>
                <small class="text-muted">Overview of your income and spending</small>
            </div>

            <button class="btn btn-sm btn-outline-primary rounded-pill"
                    data-bs-toggle="modal"
                    data-bs-target="#editSalaryModal">
                <i class="bi bi-pencil"></i> Edit
            </button>
        </div>

        @* ===============================
           KPI CARDS
           =============================== *@
        <div class="row g-2 mb-3">
            <div class="col-12 col-md-4">
                <div class="kpi-card bg-success p-3">
                    <div>
                        <h6 class="mb-1">Net Income</h6>
                        <h4 class="fw-bold mb-0">£@Model.Salary.NetSalary</h4>
                        <small>£@Model.Salary.SalaryMonthly.NetSalary/mo</small>
                    </div>
                    <i class="bi bi-graph-up-arrow"></i>
                </div>
            </div>

            <div class="col-12 col-md-4">
                <div class="kpi-card bg-warning p-3">
                    <div>
                        <h6 class="mb-1">Outgoings</h6>
                        <h4 class="fw-bold mb-0">£@Model.YearlyTotalOutGoings</h4>
                        <small>£@Model.MonthlyOutGoings/mo</small>
                    </div>
                    <i class="bi bi-credit-card"></i>
                </div>
            </div>

            <div class="col-12 col-md-4">
                <div class="kpi-card bg-primary p-3">
                    <div>
                        <h6 class="mb-1">Left Over</h6>
                        <h4 class="fw-bold mb-0">£@Model.YearlyLeftOver</h4>
                        <small>£@Model.MonthlyLeftOver/mo</small>
                    </div>
                    <i class="bi bi-piggy-bank"></i>
                </div>
            </div>
        </div>

        @* ===============================
           SALARY BREAKDOWN
           =============================== *@
        <div class="card border-0 shadow-sm rounded-3 mb-3 overflow-hidden">
            <div class="card-header bg-body-secondary px-3 py-2">
                <h6 class="mb-0 fw-semibold" style="font-size: 0.9rem;">Salary Breakdown</h6>
            </div>

            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th class="fw-bold" style="font-size: 0.8rem;">Item</th>
                            <th class="fw-bold text-end" style="font-size: 0.8rem;">Yearly</th>
                            <th class="fw-bold text-end" style="font-size: 0.8rem;">Monthly</th>
                            <th class="fw-bold text-end" style="font-size: 0.8rem;">Weekly</th>
                        </tr>
                    </thead>
                    <tbody style="font-size: 0.85rem;">
                        <tr>
                            <td>Gross Salary</td>
                            <td class="text-end">£@Model.Salary.GrossSalary</td>
                            <td class="text-end">£@Model.Salary.SalaryMonthly.GrossSalary</td>
                            <td class="text-end">£@Model.Salary.SalaryWeekly.GrossSalary</td>
                        </tr>
                        <tr class="fw-semibold bg-light">
                            <td>Net Salary</td>
                            <td class="text-end">£@Model.Salary.NetSalary</td>
                            <td class="text-end">£@Model.Salary.SalaryMonthly.NetSalary</td>
                            <td class="text-end">£@Model.Salary.SalaryWeekly.NetSalary</td>
                        </tr>
                        <tr>
                            <td>Tax</td>
                            <td class="text-end">£@Model.Salary.Tax</td>
                            <td class="text-end">£@Model.Salary.SalaryMonthly.Tax</td>
                            <td class="text-end">£@Model.Salary.SalaryWeekly.Tax</td>
                        </tr>
                        <tr>
                            <td>National Insurance</td>
                            <td class="text-end">£@Model.Salary.NationalInsurance</td>
                            <td class="text-end">£@Model.Salary.SalaryMonthly.NationalInsurance</td>
                            <td class="text-end">£@Model.Salary.SalaryWeekly.NationalInsurance</td>
                        </tr>
                        <tr>
                            <td>Pension (@Model.Salary.PensionPercentage%)</td>
                            <td class="text-end">£@Model.Salary.PensionContribution</td>
                            <td class="text-end">£@Model.Salary.SalaryMonthly.PensionContribution</td>
                            <td class="text-end">£@Model.Salary.SalaryWeekly.PensionContribution</td>
                        </tr>

                        @if (Model.Salary.HasStudentLoan)
                        {
                            <tr>
                                <td>Student Loan (@Model.Salary.StudentPlanType)</td>
                                <td class="text-end">£@Model.Salary.StudentLoan</td>
                                <td class="text-end">£@Model.Salary.SalaryMonthly.StudentLoan</td>
                                <td class="text-end">£@Model.Salary.SalaryWeekly.StudentLoan</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>

        @* ===============================
           SPENDING CATEGORIES
           =============================== *@
        <h5 class="fw-semibold mb-2">Spending Categories</h5>
        @if (Model.Category?.Count > 0)
        {
            <div class="row g-2 mb-3">
                @foreach (var cat in Model.Category)
                {
                    <div class="col-12 col-sm-6 col-lg-4">
                        <div class="card border-0 shadow-sm rounded-3 p-3">
                            <div class="d-flex justify-content-between align-items-start mb-2">
                                <h6 class="fw-semibold mb-0" style="font-size: 0.9rem;">@cat.Name</h6>
                                <span class="badge bg-success rounded-pill" style="font-size: 0.7rem;">Yearly</span>
                            </div>
                            <h5 class="fw-bold mb-2">£@cat.YearlyCost</h5>
                            <div style="font-size: 0.85rem;">
                                <div class="d-flex justify-content-between mb-1">
                                    <span class="text-muted">Monthly</span>
                                    <span>£@cat.MonthlyCost</span>
                                </div>
                                <div class="d-flex justify-content-between">
                                    <span class="text-muted">Weekly</span>
                                    <span>£@cat.WeeklyCost</span>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
        }

        @await Html.PartialAsync("_CategoryPartial", Model.Category)
    }
</div>

@* ===============================
   EDIT SALARY MODAL
   =============================== *@
@if (Model?.Salary != null)
{
    <div class="modal fade" id="editSalaryModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-md">
            <div class="modal-content border-0 rounded-4">
                <div class="modal-body p-0">
                    @await Html.PartialAsync("_salaryInput", new SalaryInputModel { SalaryId = Model.Salary.Id, GrossSalary = Model.Salary.GrossSalary })
                </div>
            </div>
        </div>
    </div>
}

<style>
    body {
        background: #f8f9fa;
    }

    .bg-glass {
        background: rgba(255,255,255,0.85);
        backdrop-filter: blur(6px);
    }

    .icon-circle {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        background: #667eea;
        color: #fff;
        display: flex;
        justify-content: center;
        align-items: center;
        font-size: 1.3rem;
        margin: auto;
    }

    .kpi-card {
        border-radius: 1rem;
        padding: 1rem;
        color: #fff;
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: .9rem;
    }

        .kpi-card i {
            font-size: 1.8rem;
            opacity: 0.5;
        }

    .category-tile {
        transition: transform .15s ease, box-shadow .15s ease;
    }

        .category-tile:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(0,0,0,0.1);
        }
</style>
